# Local development. Docker compose sets DATABASE_URL itself; this file is for
# running the api or a worker directly on the host against the compose postgres.
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/webhook_recovery
