# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/

# Node
node_modules/
frontend/dist/
frontend/.vite/
npm-debug.log*
yarn-error.log*

# Env / secrets
.env
.env.*
!.env.example

# Editors / OS
.idea/
.vscode/
.DS_Store

# Claude -- worktrees, and local working notes like .claude/OUTSTANDING.md.
# Nothing under here is tracked, so ignoring the whole directory keeps working
# notes about known defects out of a repo that is itself the submission.
.claude/
